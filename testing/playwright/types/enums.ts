export enum MigrationType {
  COLD = 'cold',
  WARM = 'warm',
}

export enum ProviderType {
  VSPHERE = 'vsphere',
  OVIRT = 'ovirt',
  OVA = 'ova',
  OPENSTACK = 'openstack',
}

export enum EndpointType {
  VCENTER = 'vcenter',
  ESXI = 'esxi',
}
