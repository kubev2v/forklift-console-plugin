name: Test
on:
  pull_request:
permissions:
  contents: read
env:
  CONTAINER_CMD: docker
  FORKLIFT_PLUGIN_IMAGE: localhost:5001/forklift-console-plugin:latest
  OKD_CONSOLE_YAML: ./ci/yaml/okd-console.yaml

jobs:
  audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: npm audit
        # Filters Console SDK-constrained packages (react-router, immutable).
        # See ci/npm-audit.sh for the allowlist and rationale.
        run: bash ci/npm-audit.sh

  linters:
    name: Run linters
    runs-on: ubuntu-latest
    steps:
      - name: Increase File Descriptors Limit
        run: ulimit -n 4096

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      # Playwright + k8s client types live under testing/ (separate package).
      # Type-aware ESLint for testing/playwright requires them (MTV-6509).
      - name: Install testing dependencies
        run: npm --prefix testing ci

      - name: Lint, eslint and stylelint
        run: npm run lint

      - name: Lint, test i18n files
        run: npm run test:i18n

      - name: Knip
        run: npm run knip

  build:
    name: Build project
    runs-on: ubuntu-latest
    steps:
      - name: Increase File Descriptors Limit
        run: ulimit -n 4096

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

  test:
    name: Run tests and coverage
    runs-on: ubuntu-latest
    steps:
      - name: Increase File Descriptors Limit
        run: ulimit -n 4096

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm run test:coverage

      - name: Test coverage
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}

  e2e:
    name: Run e2e tests
    runs-on: ubuntu-latest
    env:
      BASE_ADDRESS: http://localhost:30080
    steps:
      - name: Increase File Descriptors Limit
        run: ulimit -n 4096

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Install testing dependencies
        run: cd testing && npm ci

      - name: Install Playwright browsers
        run: cd testing && npx playwright install --with-deps chromium

      - name: Check downstream version gating
        run: cd testing && npm run check:version-gating

      - name: Run pre e2e test tasks
        run: npm run e2e:pre-test

      - name: Wait for application
        run: |
          timeout 300 bash -c 'until curl -f http://localhost:30080; do sleep 5; done'

      - name: Run Playwright tests (upstream only)
        run: cd testing && npm run test:upstream
        env:
          BASE_ADDRESS: http://localhost:30080

      - name: Upload Playwright test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: always()
        with:
          name: playwright-test-results
          path: |
            testing/test-results/
            testing/playwright-report/

