import { t } from '@utils/i18n';

export const TARGET_LABELS_MODAL_DESCRIPTION = t(
  'Specify custom labels that will be applied to all target virtual machines after migration. This can apply organizational or operational labels to migrated VMs for further identification and management.',
);
