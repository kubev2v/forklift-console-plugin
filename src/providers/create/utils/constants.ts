import { t } from '@utils/i18n';

export const CREDENTIALS_TITLE = t('Provider credentials');

export const ProjectNameSelectPopoverHelpContent = t(
  'The project that your provider will be created in.',
);
