import { t } from '@utils/i18n';

export const PROVIDER_ISSUES_TITLE_LABEL = t('Provider issues');

export const PROVIDER_ISSUES_DESC_LABEL = t(
  'View critical and warning conditions currently impacting this provider.',
);
