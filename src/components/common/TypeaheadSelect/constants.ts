import { t } from '@utils/i18n';

export const DEFAULT_NO_OPTIONS = t('No options available');
export const DEFAULT_PLACEHOLDER = t('Select an option');

export const PLACEHOLDER_VALUES = {
  NO_OPTIONS: 'NO_OPTIONS',
  NO_RESULTS: 'NO_RESULTS',
} as const;
