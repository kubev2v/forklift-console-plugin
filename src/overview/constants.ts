import { t } from '@utils/i18n';

export const OverviewTabName = {
  Health: t('Health'),
  History: t('History'),
  Overview: t('Overview'),
  Settings: t('Settings'),
  YAML: t('YAML'),
} as const;
